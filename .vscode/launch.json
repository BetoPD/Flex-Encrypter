{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug from Makefile",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/encrypter", // Ajusta el nombre del ejecutable
      "args": ["./encrypt/test.txt"], // Ajusta los argumentos
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "lldb",
      "preLaunchTask": "Build with Makefile"
    }
  ]
}
