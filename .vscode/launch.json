{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Encrypter",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/encrypter", // Ajusta el nombre del ejecutable
      "args": [
        "./encrypt/Original_document.txt",
        "./decrypt/result.txt",
        "encrypt"
      ], // Ajusta los argumentos
      "stopAtEntry": true,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "lldb",
      "preLaunchTask": "Build with Makefile"
    }
  ]
}
